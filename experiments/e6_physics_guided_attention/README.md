# E6 Physics-Guided Attention

This experiment stage compares the current hybrid Transformer against
Physics-Guided Transformer variants:

1. current hybrid baseline
2. PGT attention-bias only
3. PGT soft-mask only
4. PGT physics-loss only
5. full PGT

Store metrics, plots, checkpoints, and attention diagnostics in separate
subfolders per variant.

